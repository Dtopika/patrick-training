{
  "name": "patrick-training",
  "private": true,
  "version": "7.3.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "release:prepare": "node scripts/release-version.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
